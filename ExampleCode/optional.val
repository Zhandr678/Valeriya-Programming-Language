int j = 5;
double i = j;

struct Vector
{
	int buffer[];
	uint size;
}

fn PushBack(inout Vector v, int x) -> void
{
	v.buffer[size + 1] = x;
	v.size = v.size + 1;
}

fn Size(Vector v) -> uint
{
	return v.size;
}

fn PopBack(inout Vector v) -> void
{
	v.size = v.size - 1;
}

fn At(Vector v, uint i) -> int
{
	return v.buffer[i];
}

property prop
{
	opt1: { int x; }
	opt2: { double d; }
	opt3: { string s; }
}

prop p;

// Here you dereference 'p'
match(p)
{
	case opt1 -> { p.x; }
}

struct ListNode
{
	ListNode next;
	int val;
}

int i = null;
ListNode head = ListNode{ null, 1 };

head.next = ListNode { ListNode{ null, 3 }, 2 };

fn Traverse(ListNode head) -> void
{
	while (head != null)
	{
		printf(head.val);
		head = head.next;
	}
}